# show-toc

You are executing the `show-toc` workflow. This is a child skill of the `tech-brain` skill.

## Objective
To generate and display a table of contents for all documents within the `TECH-SECOND-BRAIN` directory.

## Procedures

1.  **Execute Script**: You MUST execute the Python script located at `scripts/list_documents.py`.
2.  **Output**: The script will query Google Drive and generate a clean, recursive list of all documents with their relative paths. You MUST display this list as the table of contents.

For full details on constraints and environment, refer to the parent `tech-brain` skill instructions.
